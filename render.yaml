services:
  - type: web
    name: p2p-share
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 wsgi:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
